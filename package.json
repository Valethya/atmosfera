{
  "name": "atmosfera-landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm install --no-save --package-lock=false puppeteer-core@latest @sparticuz/chromium@latest && node scripts/perf-ab.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
